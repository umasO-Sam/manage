# 運用ランブック — 不具合対応・改善作業をすぐ再開するために

本番稼働: <https://manage.saito-koken.co.jp>（利用開始 2026-08-07）
デプロイ手順の詳細は [DEPLOY.md](DEPLOY.md) 7章。ここは**日々の対応で見る場所だけ**をまとめる。

---

## 1. 朝いちの健康チェック（3分）

```bash
# 本番の状態・当日のエラー件数・稼働コミット
ssh -i ~/.ssh/xserver_manage -p 10022 saitokoken@saitokoken.xsrv.jp \
  'cd ~/manage && ([ -f storage/framework/down ] && echo メンテナンス中 || echo 公開中) \
   && echo "本日のERROR: $(grep -c "^\[$(date +%Y-%m-%d).*ERROR" storage/logs/laravel.log || echo 0)件" \
   && git log --oneline -1'
```

エラーが出ていたら中身を見る（`userId` で誰の操作かが分かる）:

```bash
ssh -i ~/.ssh/xserver_manage -p 10022 saitokoken@saitokoken.xsrv.jp \
  'cd ~/manage && grep -n "^\[$(date +%Y-%m-%d).*ERROR" storage/logs/laravel.log | tail -5 | cut -c1-400'
```

> ログは `storage/logs/laravel.log` の単一ファイル。肥大化してきたら日次ローテーションを検討する。

---

## 2. 開発環境

```bash
cd C:/Users/OSAMU/claude/manage
php artisan serve --port=8123      # http://127.0.0.1:8123
```

- DBは `database/database.sqlite`（本番はMySQL）
- **権限切替（開発用）**: 右上のユーザーメニューから、自分のロールと各フラグ（上長・日報管理者・勤怠管理者・役員・資金管理者・administrator）をその場で変更できる。本番ではルートごと存在しない
- **開発環境は上部メニューが薄い黄色**（本番は白）。ここで本番と見分ける。
  ファビコンは開発環境でも本番と同じものを出す（見え方を確認できるようにするため）
- テスト: `php artisan test`（現在 **633件パス**）。修正したら必ず通してからコミットする
- 見積番号の付け方は [docs/見積番号の付与規約.md](docs/見積番号の付与規約.md)（社内規約の書き起こし）

---

## 3. 本番反映

**前提**: ローカルでテストが全件通っていること。`main` にコミット済みであること。

**CSS/JSに変更が無ければフロントの転送は不要**（PHP・Blade だけの修正はこれに当たる）。
ビルド済みCSSに含まれないTailwindクラスを新しく使った場合だけ、下の「フロントも送る場合」を足す。

```bash
cd C:/Users/OSAMU/claude/manage
php artisan test          # 全件通ってからpushする
git push origin main

# 本番の作業ツリーは常に origin/main と同一にする（本番側で編集はしない前提）。
# git pull は作業ツリーに差分があると止まるため、reset --hard で確実に合わせる。
ssh -i ~/.ssh/xserver_manage -p 10022 saitokoken@saitokoken.xsrv.jp 'cd ~/manage \
  && /usr/bin/php8.3 artisan down --render="errors::503" \
  && git fetch origin && git reset --hard origin/main \
  && /usr/bin/php8.3 artisan migrate --force \
  && /usr/bin/php8.3 artisan config:cache && /usr/bin/php8.3 artisan route:cache && /usr/bin/php8.3 artisan view:cache \
  && /usr/bin/php8.3 artisan up && git log --oneline -1'
```

**マイグレーションを伴う場合は、先に本番DBのバックアップを取る**:

```bash
ssh -i ~/.ssh/xserver_manage -p 10022 saitokoken@saitokoken.xsrv.jp 'cd ~/manage && mkdir -p ~/backups \
  && eval "$(/usr/bin/php8.3 artisan tinker --execute='"'"'
       $c = config("database.connections.mysql");
       echo "export DBH=".escapeshellarg($c["host"]).";";
       echo "export DBU=".escapeshellarg($c["username"]).";";
       echo "export MYSQL_PWD=".escapeshellarg($c["password"]).";";
       echo "export DBN=".escapeshellarg($c["database"]).";";
     '"'"' 2>/dev/null | tail -1)" \
  && mysqldump -h "$DBH" -u "$DBU" "$DBN" > ~/backups/manage_$(date +%Y%m%d_%H%M%S).sql \
  && ls -lh ~/backups/ | tail -2'
```

<details><summary>フロントも送る場合（CSS/JSを変更したとき）</summary>

`npm run build` は **PowerShell で `dev-tools-path.ps1` を読み込めば実行できる**
（node/npm は既定のPATHに無い。Bashツールからは使えないので PowerShell で行う）。

```powershell
. C:\Users\OSAMU\claude\dev-tools-path.ps1   # node v24 / npm 11 がPATHに入る
npm run build
```

転送は上書き展開のため、本番の `public/build/assets/` には**過去のビルド成果物が溜まっていく**
（2026-08-17時点で16ファイル）。使われるのは `manifest.json` が指す2つだけなので実害は無いが、
気になったら manifest が指すファイル以外を消す。**反映直後は消さない**（古いページを開いたままの
利用者がいる）。

```bash
tar czf build.tar.gz -C public build
scp -i ~/.ssh/xserver_manage -P 10022 build.tar.gz saitokoken@saitokoken.xsrv.jp:~/manage/
rm -f build.tar.gz
# 本番側の展開を reset --hard の直後に挟む:
#   && tar xzf build.tar.gz -C public && rm build.tar.gz \
```

</details>

反映後は必ず**1章の健康チェック**を実行する。

### やってはいけないこと

- **`composer install` を実行しない**。本番のcomposerは1.9.1で、Laravel 13が要求する
  `composer-runtime-api ^2.2` を満たせず落ちる。依存を変更した場合はローカルで
  `vendor/` を作って転送する（今のところ依存の変更は発生していない）
- キャッシュを消したまま放置しない。`config:cache` 済みの状態が正
- 静的ファイル（ファビコン等）はサーバー側にもキャッシュが残る。差し替えたら
  HTMLの `?v=` を上げる。バージョンなしのURLはしばらく古い内容を返すことがある。
  ファビコンの作り直しは `php tools/make-favicon.php <ロゴ画像> public`（5ファイルを再生成）
- **本番のIDレコードを勝手に変更しない**。権限・氏名などの変更は都度確認を取る

### 切り戻し

```bash
ssh -i ~/.ssh/xserver_manage -p 10022 saitokoken@saitokoken.xsrv.jp 'cd ~/manage \
  && /usr/bin/php8.3 artisan down && git reset --hard <戻したいコミット> \
  && /usr/bin/php8.3 artisan config:cache && /usr/bin/php8.3 artisan view:cache \
  && /usr/bin/php8.3 artisan up'
```

マイグレーションを伴う変更を戻す場合は、**先に**`migrate:rollback --step=N --force` を実行する。
データを消す方向のロールバックは影響を確認してから。

---

## 4. 権限の全体像（不具合の切り分けで最初に見る）

ロールは3つ。上に重ねる**フラグ**で権限が増える。

| ロール | 主な範囲 |
|---|---|
| 経理資材担当 | カード移動、仕入管理の全機能、担当者管理 |
| 営業担当 | 各ボード、仕入管理の検索・原価計算、物件管理 |
| 一般社員 | 購入手配・見積依頼ボード、履歴、自分の勤怠 |

| フラグ | 効果 |
|---|---|
| 上長 | 他人の勤怠・原価の閲覧、申請承認、作業日報一覧・確認の閲覧 |
| 日報管理者 | 作業日報の**確認・差し戻し**と未確認バッジ（経理資材担当に付ける） |
| 勤怠管理者 | 承認済み申請の取消の**反映判断**、**休日勤務申請の最終承認**（9章）、**作業日報の代理入力**（11章）、**休暇・休出の代理申請**（13章）。「勤怠管理者の確認」画面とバッジ |
| 名簿に表示しない | 各画面の担当者リストから除外（ＩＤ管理には残る） |
| 役員 | 物件管理ボード、原価、役員フラグの付与 |
| 資金管理者 | 取引先一覧、入金済の非表示。経理資材担当と同等の扱い |
| administrator | すべての機能。administratorのみが付与できる |

付与のはしご: 経理資材担当 ＜ 役員 ＜ 資金管理者 ＜ administrator。
**自分より上の権限のアカウントは編集・削除できない**（IDとパスワードが見えるため）。

**勤怠管理者フラグだけは、このはしごとは別の判定**。付け外しできるのは
役員・勤怠管理者・administrator のみで、経理資材担当・資金管理者は操作できない。
ただし**このフラグだけではＩＤ管理画面に入れない**（パスワードを再設定できる画面のため、
開ける範囲は経理資材担当・役員・資金管理者のまま）。フラグを持つ人が自分で付け外しするには、
そのいずれかを兼ねている必要がある。

---

## 5. 本番データの現況（2026-08-07 時点）

| 対象 | 件数 |
|---|---|
| 担当者ID | 32 |
| 注番マスタ | 37（全件プルダウン表示） |
| 受注ヘッダ | 1,743（合計 ¥17,362,901,931） |
| 仕入明細 | 242,030 |
| 調達ボードのカード | 51 |
| 物件カード | 0（運用開始待ち） |
| 作業日報 | 1（運用開始待ち） |
| 見積台帳（過去注番） | 9,196 |

日報管理者: 管理アカウント／瀧上祥子／水上留美子／柴田拓弥／斉藤央奈（+ administrator の斉藤修）
勤怠管理者: 柴田拓弥／斉藤富美／斉藤央奈（+ administrator の斉藤修・管理アカウント）

**受注ヘッダ1,743件は取引先・担当者・カードがいずれも未設定**。仕入管理から引き継いだ
過去データで、物件管理に載せるのは一部だけの想定。取引先マスタも0件のため、移行時は
受注先と社内担当者を都度入力することになる（受注先名は仕入明細の値が初期値に入る）。

---

## 6. 自動実行（cron）

`*/5 * * * * php8.3 artisan schedule:run` が設定済み。

| 時刻 | 処理 |
|---|---|
| 2:00 | `app:archive-completed-cards` 完了カードを履歴へ |
| 2:15 | `app:purge-archived-cards` 保存期間を過ぎた履歴を削除 |
| 2:30 | `app:prune-operation-logs` 操作ログの整理 |

物件管理ボードは `retention_days = null` のため、この自動処理の対象外
（入金済でも人が非表示にするまで残す）。

---

## 7. 積み残し

- **テストアカウント（test）・管理アカウント（admin）の「名簿に表示しない」が未設定**。
  既存レコードの変更のため、指示待ち
- **新サーバー簡単移行**（Xserverの案内分）。休日出勤のない日に別途計画する。
  現環境は sv8637。**手順とチェックリストは12章**
- ログの日次ローテーション未設定
- **勤怠管理画面でセルをクリックしてその日のステータスを強制変更する機能は不採用**（2026-08-07）。
  勤務状況一覧は申請から導出するだけの読み取り専用ビューで、実装には別テーブルが要る

---

## 8. 利用開始後に出やすい問い合わせと確認先

| 症状 | 最初に見る場所 |
|---|---|
| メニューに項目が出ない | 4章の権限表。ロールとフラグの組み合わせを確認 |
| 作業日報確認で確認ボタンが出ない | 日報管理者フラグ。閲覧だけなら上長・役員でも可 |
| 調達ボードのバッジが減らない | 経理資材担当・administratorは**全カード**を数える仕様。「自分の依頼以外を既読にする」で片付ける |
| 注番がプルダウンに出ない | 注番管理の「プルダウン」チェック。各画面の「非表示の注番も表示する」で一時的に選べる |
| 打刻と日報の乖離警告が出ない | `timecard` 接続とSIDの一致（SIDを正とする） |
| 仕入管理の検索で注番が0件になる（ZATSU など） | **人工にしか無い注番**。仕入明細には1件も無い（ZATSU＝雑人工で本番2万件超、他に `3D勉強会` `AC-05` など多数）。注番で検索すると「人工データ」の別表に並ぶ（2026-08-10）。人工が持たない項目（品名・製品名・型式・メーカー・商社名・各日付）で絞り込むと別表は出ない |
| エクセル一括登録の貼り付け欄でセルを移動したい | ↑↓・Enterで上下、Tab / Shift+Tabで左右（2026-08-17）。←→ はセル内の文字カーソルのまま。日本語変換中の↑↓・Enterはセルを動かさない |
| 画面が真っ白 | 1章のエラーログ。`view:cache` の作り直しで直ることが多い |
| 承認済みの申請を取り消したい | 申請詳細の「取消を申請する」。上長の承認後、勤怠管理者が反映して初めて確定する（9章） |
| 休日勤務を上長が承認したのに「承認待ち」のまま | 仕様（2026-08-10）。休日勤務だけは勤怠管理者の確認を経て承認済みになる（9章）。勤務状況一覧・カレンダーには橙の「承認待ち」として出る |
| 休日勤務が有給残・全社の休暇予定に出てこない | 勤怠管理者が承認するまでは承認済みではないため。振替休日も同様に未確定扱い（9章） |
| 仕入管理で入れた人工が原価計算に出てこない | 仕様（2026-08-10）。作業日・担当者からその日の日報にぶら下がり、**作業日報の確認で確認されるまでは未確認**。確認画面の「仕入管理のデータ入力から登録された人工」の欄で確認する |
| 日報を出し直したら仕入管理で入れた人工が消えた | 解決済み（2026-08-10）。`labor_costs.origin` で見分け、日報のグリッド由来の分だけ作り直す |
| 日報の差し戻しに気づかれない | 差し戻し時にメールが届く（2026-08-10）。確認（承認）時のメールは出さない仕様。代理提出分は代理提出者に届く |
| 差し戻した人工がどこにも出てこない | 人工レコード確認の一覧の下の「差し戻し ○件」の枠（2026-08-17）。確定していないので上の一覧には出ない。「日報を開く」でその日の作業日報確認へ飛べる |
| 人工レコードの修正で右側の項目が見えない | 解決済み（2026-08-10）。修正フォームは画面幅に収まり、横スクロール不要 |
| エラーメッセージに英語の項目名が出る | `lang/ja/validation.php` の `attributes` への登録漏れ。`ValidationAttributeNameTest` が検出する |
| 本人が日報を出せない（入院・退職・PCが無い等） | 勤怠管理者が作業日報画面の「代理入力する担当者」で本人を選んで代理提出する（11章） |
| 本人が休暇・休出を申請できない | 勤怠管理者が休暇・休出申請画面の「代理申請」から出す（13章）。承認者と本人の双方に通知が届く |
| 代理申請しようとしたら有給の残日数が足りないと出る | **対象者を選んでから**種別を選ぶ。対象者未選択のまま進むと自分の申請になる。残数の判定は対象者で行う（13章） |
| 代理提出した日報を差し戻したのに本人が知らない | 仕様（11章）。代理提出分の差し戻しは**本人ではなく代理提出した勤怠管理者**に返る。代理提出者の作業日報画面の上部に一覧が出る |
| 取消したのに勤務状況一覧から消えない | 仕様。勤怠管理者が反映するまで承認済み扱いのまま残る（9章） |
| 取消の反映確認が誰にも回らない | 勤怠管理者フラグが0人になっていないか。4章 |
| 一括承認のチェックボックスが出ない | 承認待ちが0件のとき。取消申請の欄は一括対象外（1件ずつ判断する） |
| 物件カードが「注番はすでに登録されています」で作れない | **受注ヘッダがある**注番。作成画面の「すでに受注済みの案件を登録する」から検索して取り込む。注番管理に登録済みなだけなら、そのまま受注登録できる（2026-08-10 以降） |
| 採番した注番を注番管理に追加したら受注登録できない | 解決済み（2026-08-10）。注番マスタにある注番でも受注登録でき、マスタのレコードを共通で使う（受注前の採番を想定）。重複は受注ヘッダ側だけで見る |
| 部品(B)・改造(K)・修理(S)を選ぶと別の装置の番号(老番+1)になる | 解決済み（2026-08-17）。「TL091」のように**通番まで入れて検索**すれば、その装置の番号で採番する。元の見積番号は実在する注番から選ぶ（`docs/見積番号の付与規約.md`） |
| 元の見積番号の候補に古い注番が混ざる／認識されない | 台帳に規約と逆順の表記が残っている（`TL02-B01N02` は `N02B01` が正）。本番で2件。見つけたら台帳側を直す |
| 同じ注番なのに注番管理と見積番号台帳で別物になる | 注番の英字は入力時に大文字へ揃える（採番・注番管理・受注登録の3画面。2026-08-10）。形式チェックを解除した注番だけは入力のまま登録される |
| カレンダーアイコンを押しても開かない | 解決済み（2026-08-07）。古いキャッシュが残っていたら `view:cache` を作り直す |
| パスワードを変えたのに別の端末が使えたまま | 解決済み（2026-08-07）。変更前から続いているセッションは次の操作でログイン画面に戻る。**本番反映前にログインした端末は一度だけそのまま**（古いセッションに照合用のハッシュが無いため） |
| スマホで作業日報をなぞって選択できない | 仕様（2026-08-07）。スマホは**始めの時間帯をタップ→終わりの時間帯をタップ**。なぞる操作は縦スクロールと奪い合いになるため採用していない。離れた時間帯は「＋追加」ボタン（PCのCtrlキーと同じ） |
| なぞって選択で休憩が消せない | 解決済み（2026-08-07）。「削除」を選んでなぞれば休憩も消える。作業内容の反映では従来どおり休憩を残す |
| 作業日報で「この分類は注番を選択しないと反映できません」が出る | 注番なしで入れられるのは**62:打合見積／69:研修など／70:管理／71:空き**だけ。定義は `CategoryCode::ORDER_NO_OPTIONAL_CODES` の1箇所（画面もサーバーもここを見る） |

---

## 9. 承認済み申請の取消（3段階）

```
承認済み ──[本人が理由を書いて取消申請]──→ 取消申請中
                                              │
              ┌───[上長が差し戻し]────────────┤
              ↓                               ↓[上長が承認]
          承認済みに戻る              取消の反映確認中 ──[勤怠管理者が反映]──→ 取消済み
              ↑                               │
              └───[勤怠管理者が差し戻し]───────┘
```

- **反映されるまで status は承認済みのまま**。勤務状況一覧・個人カレンダー・有給残日数は
  いずれも承認済みかで判定しているため、途中で消すと「取り消せるか未確定なのに休みが消える」
- 差し戻すと承認済みに戻り、本人はもう一度取消を申請できる（別の申請を出し直す判断も可）
- 上長は「申請承認」画面の下段、勤怠管理者は「取消の反映確認」画面で判断する
- 経過は操作ログに5種類のアクションで残る（取消申請／上長の承認・差し戻し／反映・差し戻し）

承認前の取り下げは従来どおり申請詳細の「取消」ボタン（`withdrawn`）で、このフローは通らない。

### 休日勤務申請の2段階承認（2026-08-10〜）

```
申請 ──[上長が承認]──→ 承認待ち（勤怠管理者） ──[勤怠管理者が承認]──→ 承認済み
       │                        │
       └[上長が却下]→ 却下       └[勤怠管理者が差し戻し]→ 却下（本人＋上長に通知）
```

- **休日勤務申請（`holiday_work`）だけ**が2段階。定義は `LeaveRequest::ATTENDANCE_APPROVAL_TYPES` の1箇所
- 上長が承認した時点の status は `pending_attendance`。**承認済みではない**ので、有給残日数・
  全社の終日休み判定（振替休日を含む）・個人カレンダーの背景色には反映されない
- 勤務状況一覧・個人カレンダーには**橙色の「承認待ち」**として出る（上長が通した予定を現場が
  把握できるようにするため）。判定は `LeaveRequest::PENDING_STATUSES`
- 勤怠管理者の差し戻しは**却下**（`rejected`）。上長の判断を覆すため、本人と上長の双方に通知する
- 決裁が終わるまでは、勤怠管理者待ちの間も本人が「取消」で取り下げられる
- 一括承認でも同じく勤怠管理者へ回る

---

## 10. 作業日報まわりの決めごと（2026-08-10〜）

### 仕入管理から入れた人工も「その日の日報」になる

社内人工（日報）登録・エクセル一括登録（人工）で入れた人工は、**作業日と担当者から
その日の日報にぶら下がり**、作業日報の確認対象になる。1人1日1枚なので、本人が出した
日報があればそこに合流する。

- 確認されるまでは `is_provisional = true`（未確認）。**原価計算にはまだ乗らない**
- 確認画面では、時間帯を持たないため「仕入管理のデータ入力から登録された人工」という
  別表で並ぶ。時間帯グリッドは本人が日報で入れたコマがあるときだけ出す
- 作業日か担当者が欠けている仮登録は日報を特定できないため紐づけない
- **差し戻しても人工レコードは消えない**。未確認（`is_provisional = true`）のまま残り、原価計算には
  乗らない。差し戻しが変えるのは日報の状態だけ。ただし差し戻し中は確認待ちの集計から外れるため、
  **人工レコード確認の一覧の下に「差し戻し ○件」の別枠**を出して拾えるようにしている（2026-08-17）。
  仕入管理から入れた分は本人が日報を出し直しても直らないので、この枠から日報を開いて対処する
- **`labor_costs.origin`** が `daily_report`／`purchase_input` を持つ。本人が日報を出し直したときの
  作り直し（`DailyReportController::syncLaborCosts`）は `daily_report` の分だけを消す。
  `daily_report_id` の有無で見分けると、仕入管理から入れた分まで消えるので触らないこと
- 人工レコード確認の「登録元」バッジ・絞り込みもこの `origin` を見る

### 差し戻しの通知

作業日報が差し戻されたときだけメールを送る（`DailyReportRejectedMail`）。**確認（承認）時は
送らない**（ユーザーの指示。差し戻しだけが相手に行動を求めるため）。代理提出されたものは
本人ではなく代理提出者に届く。送信に失敗してもログに残すだけで、差し戻し自体は成立する。

### 入力エラーの項目名

`lang/ja/validation.php` の `attributes` に日本語名を集約している。登録が漏れると英語の
カラム名がそのまま出るため、`ValidationAttributeNameTest` が `validate()` と `rules()` の
項目を走査して漏れを検出する。**新しい入力項目を足したらここにも追加する。**

---

## 11. 作業日報の代理入力（2026-08-10〜）

勤怠管理者・administrator は、作業日報画面の「代理入力する担当者」で本人を選ぶと、
その人の日報を入力・提出できる。それ以外の人にはこの選択欄が出ず、従来どおり自分の分だけ。

- 提出物は**対象者の日報・対象者の人工データ**になる。確認（日報管理者）の対象になるのも通常どおり
- 誰が代理で出したかは `daily_reports.proxy_staff_id` に持つ。確認画面には「代理提出（氏名）」と出る
- **差し戻しは本人ではなく代理提出者に返る**。代理提出者の作業日報画面の上部に、差し戻された
  分の一覧（日付・対象者・理由）が出て、そこから直接開いて直せる
- **本人も自分の画面から上書きできる**。本人が出し直すと代理の印は外れ、以降の差し戻しは本人に返る
- 操作ログは「作業日報を代理提出（勤怠管理者）」として、誰が誰の分を出したかを備考に残す

---

## 12. 新サーバー簡単移行（Xserver）

現環境 `sv8637.xserver.jp`。**移行はサーバー単位**なので、下の3ドメインと manage が同時に移る。

| 対象 | 構成 | 壊れやすさ |
|---|---|---|
| **manage**（manage.saito-koken.co.jp） | Laravel。`.env` に接続先を直書き | **高。要手当て** |
| saito-koken.co.jp（会社HP・採用） | 静的HTML＋PHPフォーム、WordPress(`koichi2`) | 低 |
| hakubosha.com | 静的HTML中心 | ほぼ無し |
| saitokoken.xsrv.jp | PHP＋DB接続 | 低（自動修正の対象） |

### 最重要: `.env` は自動修正されない

Xserverが自動で書き換えるのは **`.php` / `.cgi` / `.pl` / `.yml` / `.ini`**。
**`.env` は拡張子が無く対象外**なので、切り替え後に手で直す。

```
DB_HOST=mysql8064.xserver.jp   ← 変わると DB に繋がらず manage が落ちる
MAIL_HOST=sv8637.xserver.jp    ← 変わると申請の通知メールが止まる
```

`MAIL_HOST` は特に危険。`sendNotification()` は送信に失敗してもログに残すだけで
エラーにしない設計のため、**誰も気づかないまま通知だけ止まり続ける**。

会社HPのフォーム（`recruit/mail.php` 等）は PHP の `mail()` を使いホスト名を
持たないので、この問題は起きない。WordPress の `wp-config.php` は `.php` なので自動修正される。

### 事前準備（切り替え前日まで）

- [ ] DBバックアップ（3章のコマンド）をローカルへ取得
- [ ] `.env` の控えを取る
- [ ] 現在値をメモ: ホスト名 `sv8637`、`DB_HOST=mysql8064.xserver.jp`、cron、SSH公開鍵
- [ ] **メールソフトの設定を先にドメイン名へ変える**（`sv86xx.xserver.jp` 直指定をやめ
      `mail.saito-koken.co.jp` 等にしておけば、移行後の設定変更が不要になる）
- [ ] `api_proxy.php` のGemini APIキーの置き場所を確認（移行対象外の場所にあると動かなくなる）

### 手順

1. **データコピー申請** — XServerアカウント →「新サーバー簡単移行」→ 対象サーバー選択。
   データ量が大きい（仕入明細24万件・DB75MB）ため数時間かかる。
   **開始からコピー完了までキャンセル不可**。この間、現サーバーは通常どおり動く
2. **動作確認（切り替え前）** — hostsに新サーバーのIPを書いて確認する。
   ファイルマネージャ・phpMyAdmin も使える。**データコピー実行ログでエラーを見る**
3. **サーバー切り替え** — Webの参照先とメールの配送先が新サーバーになる。
   **移行後14日間は元に戻せる**

### 切り替え直後のチェック（壊れやすい順）

```bash
ssh -i ~/.ssh/xserver_manage -p 10022 saitokoken@saitokoken.xsrv.jp \
  'hostname; grep -E "^(DB_HOST|MAIL_HOST)=" ~/manage/.env; crontab -l | grep -v "^#"; composer --version'
```

- [ ] **`.env` の `MAIL_HOST` を新ホスト名に更新**（`DB_HOST` も変わっていれば更新）
- [ ] `php artisan config:cache` で再キャッシュ（`.env` を変えたら必須）
- [ ] **cron** — コマンドパス `/usr/bin/php8.3` が変わる可能性。`schedule:run` が動くか
- [ ] **SSH公開鍵** — 引き継がれない場合は再登録（デプロイが止まる）
- [ ] **採用フォームのテスト送信** — `mail()` は失敗しても画面上は成功に見える。
      応募が届かない事故が一番痛いので必ず実際に送る
- [ ] manage: ログイン→調達ボード表示（＝DB接続）→申請登録→**通知メール受信**
- [ ] `api_proxy.php`（AI機能）の動作
- [ ] WordPress（`koichi2`）の表示
- [ ] 1章の健康チェック

### ついでに確認すると良いこと

本番の composer は 1.9.1 と古く `composer install` を実行できない（3章）。
新環境では更新されている可能性があり、**依存を変更できるようになるかもしれない**。

### メールについて

- 新仕様サーバーは**メール送信に SMTP認証（SMTP-AUTH）が必須**
- 切り替え後 数時間〜24時間は新旧どちらのサーバーで受信されるか読めない。
  この間は**両方で受信できる状態**にしておく（公式案内）
- 移行対象外: アクセスログ・エラーログ・自動バックアップデータ

参考: [ご利用手順](https://www.xserver.ne.jp/manual/man_order_servertransfer_flow.php) /
[仕様詳細](https://www.xserver.ne.jp/manual/man_order_servertransfer_detail.php)

---

## 13. 休暇・休出の代理申請（2026-08-17〜）

勤怠管理者・administrator は、休暇・休出申請画面の「新しく申請する」の隣にある
**「代理申請」**から、本人に代わって申請を出せる。それ以外の人にはボタンが出ない。

- 申請フォームの**いちばん上で対象者を選ぶ**（種別より先に選ばせる。入力し終えてから
  選び忘れると自分の申請になってしまうため）。選ぶとその担当者の**有給残日数**が出る
- 出来上がる申請は**対象者のもの**（`staff_id` は対象者）。承認フローは通常どおり本人の
  申請として進む。誰が代理で出したかは `leave_requests.proxy_staff_id` に持つ
- **有給の残日数は対象者で判定する**。ログイン者で見ていると、対象者に残数があるのに
  勤怠管理者の残数で弾かれる（逆も起きる）
- 通知は**承認者と対象者の双方**に届く。本人の知らないところで申請が立つため
  （見出しは「代理で休暇・勤務申請が行われました」）。詳細画面とメールに代理申請者名が出る
- 代理で出した分は**申請画面の下に「代理で出した申請」として一覧**が出る。本人の一覧に
  しか出ないと、出した側が承認・却下の結果を追えない
- **休日勤務を代理申請した場合、その勤怠管理者本人が最終承認もできる**（9章の2段階承認。
  勤怠管理者が少人数のため、意図的にこの制限は掛けていない）
- 取消は従来どおり**本人**が行う（代理申請者の取消操作は用意していない）
